[theme]
primaryColor = "#6366f1"
backgroundColor = "#0f172a"
secondaryBackgroundColor = "#1e293b"
textColor = "#f1f5f9"
font = "sans serif"

[server]
headless = true
port = 8501
# Largest accepted upload, in MB (Streamlit's default is 200)
maxUploadSize = 50

[browser]
gatherUsageStats = false
